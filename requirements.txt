# Runtime dependencies for the AwareLiquid Qwen adapter.
#
# Retrieval uses a local sentence embedder (transformers + torch). Generation is
# reached over HTTP via the Python standard library, so no HTTP SDK is required.
torch>=2.0.0
transformers>=4.40.0
numpy>=1.24.0

# --- Test / dev only ---
pytest>=7.0.0
